<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2025 ACSONE SA/NV
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="contract_line_invoice_enrich_form_view" model="ir.ui.view">
        <field name="model">contract.line.invoice.enrich</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <!-- TODO -->
                </header>
                <sheet>
                    <!-- TODO -->
                    <group>
                        <field name="name" />
                    </group>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="contract_line_invoice_enrich_search_view" model="ir.ui.view">
        <field name="model">contract.line.invoice.enrich</field>
        <field name="arch" type="xml">
            <search>
                <!-- TODO -->
            </search>
        </field>
    </record>

    <record id="contract_line_invoice_enrich_tree_view" model="ir.ui.view">
        <field name="model">contract.line.invoice.enrich</field>
        <field name="arch" type="xml">
            <list>
                <!-- TODO -->
                <field name="name" />
            </list>
        </field>
    </record>

    <record id="contract_line_invoice_enrich_act_window" model="ir.actions.act_window">
        <field name="name">Contract Line Invoice Enrich</field>
        <!-- TODO -->
        <field name="res_model">contract.line.invoice.enrich</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record id="contract_line_invoice_enrich_menu" model="ir.ui.menu">
        <field name="name">Contract Line Invoice Enrich</field>
        <field name="parent_id" ref="xmlid of parent menu" />
        <!-- TODO -->
        <field name="action" ref="contract_line_invoice_enrich_act_window" />
        <field name="sequence" eval="16" />
        <!-- TODO -->
    </record>
</odoo>
